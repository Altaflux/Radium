Card {

     color: String
     pattern: String

    Card(String cardColor,String cardPattern) {
        color = cardColor
        pattern = cardPattern
    }

    fn getColor(): String {
        color
    }

    fn getPattern(): String {
        pattern
    }

    fn toString(): String {
        return "{" + color + "," + pattern + "}"
    }
}